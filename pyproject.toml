[project]
name = "colordyne-controller"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyusb>=1.3.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
