[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "colordyne-controller"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyusb>=1.3.1",
]

[tool.hatch.build.targets.wheel]
packages = ["vm116"]

[dependency-groups]
dev = [
    "ruff>=0.15.15",
]
